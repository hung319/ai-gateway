# ==============================================
# 1. BẢO MẬT & CẤU HÌNH CHUNG (BẮT BUỘC)
# ==============================================

# Key quản trị tối cao dùng để đăng nhập Panel và gọi API quyền Admin.
# QUAN TRỌNG: Hãy đổi chuỗi này thành một chuỗi ngẫu nhiên dài và bảo mật.
MASTER_KEY=sk-master-secret-change-this-to-something-secure-123

# Đường dẫn lưu file Database SQLite.
# Nếu chạy Docker, hãy giữ nguyên /data/gateway.db để nó lưu vào volume.
# Nếu chạy Local (python main.py), bạn có thể đổi thành gateway.db
DB_PATH=/data/gateway.db

# ==============================================
# 2. REDIS & CACHING (KHUYÊN DÙNG)
# ==============================================

# URL kết nối Redis.
# - Nếu dùng Docker Compose: dùng 'redis://redis:6379/0' (tên service là redis)
# - Nếu chạy Local không Docker: dùng 'redis://localhost:6379/0'
# - Nếu không muốn dùng Cache: Để trống dòng này.
REDIS_URL=redis://redis:6379/0

# Bật/Tắt tính năng cache câu trả lời (Semantic Caching)
ENABLE_CACHE=true

# ==============================================
# 3. OBSERVABILITY (LANGFUSE - TÙY CHỌN)
# ==============================================
# Nếu bạn muốn theo dõi log chat, token usage, chi phí trên Langfuse.
# Đăng ký tại langfuse.com hoặc tự host để lấy key.

LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# ==============================================
# 4. LITELLM SETTINGS
# ==============================================
# Mức độ log của thư viện LiteLLM (DEBUG, INFO, WARNING, ERROR)
LITELLM_LOG=INFO
